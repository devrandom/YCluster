# Chrony config for dev NTP server

# Sync to public NTP servers (dev laptop has internet)
pool pool.ntp.org iburst

# Allow clients on local network
allow 10.0.0.0/24

# Serve time even if not fully synced
local stratum 10

# Drift file
driftfile /var/lib/chrony/drift
