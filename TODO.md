# TODO

## Priorities (triaged 2026-06-10)

A prioritized index into the detail below; nothing here is a separate item.
Completed work has moved to `CHANGELOG.md`.

**Next (operational resilience; caused real outages or data-loss exposure):**
- DR runbooks, esp. etcd quorum loss (only unrecoverable failure mode) + a `restore` path; Ceph off-cluster backup story.
- Incus storage `dir`→`zfs` migration — runaway-guest root-fs fill (nv3 cascade).
- WG inbound ingress that follows the leader; rathole PROXY protocol for real client IPs.
- Architecture A1-A4 (bootstrap trust model, plaintext keys, setup-time RBD `--exclusive`, Go-proxy watch resilience).

**Later (hygiene, docs, breadth):**
- Ansible hygiene H1-H3; remaining Monitoring gaps; Docker pruning; CI/lint.
- Dev-cluster system tests — script the incus DNS host-record verification as the first one (see item below).
- Node-lifecycle + scaling/capacity runbooks; secrets/SSH-key rotation; audit-log story; vendor-account cleanup.
- Minor review items; exo prompt-logging upstream; vllm-mlx operational gaps.

---

- split passwords frontend from general, rotate general
- Turn the incus DNS host-record verification into an automated system test on the dev cluster. The 2026-06-10 fix was verified by hand against `dev/cluster/` (the dev nodes are pinned, never-DHCP containers — the exact lease-less case the fix targets); script it as a repeatable test, e.g. `dev/cluster/test-dns-sync.sh`: (1) `dev-cluster.sh up`; (2) baseline — clear `host-record=` lines from `ycdev0` `raw.dnsmasq`, assert `dig @10.0.0.1 s1.incus` is NXDOMAIN; (3) run `sync_dns_records()` (venv python, package from `config/ansible/admin/files/ycluster`), assert all pinned nodes resolve and a second run returns `{}`; (4) lifecycle — `incus init` a throwaway pinned instance, sync, assert it resolves; delete it, sync, assert NXDOMAIN with others intact; (5) ownership split — run the `install-incus.yml` resolver-task shell against `ycdev0`, assert host-records survive and the task is idempotent, then assert `sync_dns_records()` preserves the resolver lines; restore `raw.dnsmasq` at the end. This would be the first scripted system test on the dev cluster — structure it so further tests (etcd TLS migration rehearsal, leader election) can join a common harness (`dev/cluster/tests/`).
- Add a `ycluster vm set-gpus <name> <count>` subcommand. Changing a VM's GPU count is currently a manual procedure (done by hand for vm1 2026-06-10): graceful `ycluster vm stop`, `incus config device remove <name> gpuN ...` (or `device add` from `free_gpus()` when growing), `ycluster vm start`, then a hand-edited etcd write to update `gpus` in `/cluster/vms/<name>` — easy to get wrong or forget the registry half. The subcommand belongs in `vm_manager.py` (reuse the launch path's pool selection and device-naming convention, keep `gpu0..gpuN-1` contiguous) + `cli/vm.py`. Two pitfalls it should handle: (1) physical passthrough can't hot-(un)plug — require/perform a stop/start; (2) the restart can boot the guest into a kernel staged by unattended-upgrades since its last boot, with no NVIDIA module built for it — verify the guest sees the expected GPU count (`nvidia-smi -L` via incus exec) after start and warn loudly.
- Optional: replace the GPU-VM guests' DKMS driver build with Ubuntu's Canonical-signed precompiled modules to drop DKMS entirely (eval'd 2026-06-13, viable — the `linux-headers-virtual` fix that made DKMS reliable is already shipped, see `CHANGELOG.md`). `incus-build-gpu-image.sh.j2` currently installs `nvidia-open` from NVIDIA's CUDA repo + DKMS (`nvidia-dkms-open`, `dkms`, `linux-headers-virtual`). Ubuntu's `noble-updates/restricted` ships `linux-modules-nvidia-595-open-generic` — a tracking meta that follows the generic kernel ABI in lockstep (no DKMS, no reboot-driverless risk, secure-boot-signed; the guests' `linux-image-virtual` resolves to the generic ABI, so `-generic` matches) — and the matching userspace `nvidia-driver-595-open` is the *identical* point release (595.71.05) we run today, so no version regression on branch 595. Switch shape: drop `dkms`/`nvidia-dkms-open`/`linux-headers-*`, install `linux-modules-nvidia-<branch>-open-generic` + `nvidia-driver-<branch>-open` (templated branch var) from the archive, keep `cuda-toolkit-X` + `nvidia-container-toolkit` from the CUDA repo. Trade-off: pins the driver to Ubuntu's restricted cadence — branch 610 (already in the CUDA repo) isn't precompiled yet, so chasing the newest driver for new-GPU support would need to fall back to the DKMS path. Before rollout, verify CUDA-toolkit/userspace compat and that the `linux-image-virtual`→generic-ABI meta tracking actually pulls the new module across a real kernel bump.
- Build incus VM images once and copy them between hosts instead of building per-host. Today each VM host runs `incus-build-gpu-image` + `incus-build-vllm-image` independently (~1h each, duplicated 8-CPU/16GiB builder load, and the "identical" images actually differ per host — different build dates pick up different package versions). Incus supports server-to-server image transfer (`incus image copy` or export/import), so the playbook could build on one host and distribute: pick a build host, run the helpers there, copy to the rest, keeping aliases in sync. Prerequisite to check: image transfer needs the incus API listening between hosts (or fall back to `incus image export` + scp + import, which works with no API exposure). Per-host builds do have one virtue — no cross-host dependency — so keep the standalone helpers as the fallback path. Worth doing before the image set grows (more derived layers / bigger AOT caches make duplicate builds increasingly wasteful).
- Clean up the unmanaged searxng-docker stack on the storage leader — its `caddy` caused a real outage 2026-06-11. The stack at `/opt/src/searxng-docker` (deployed 2025-12-21, NOT repo-managed) is upstream's default compose: searxng + redis + **caddy with host networking** and `restart=unless-stopped`. Caddy crash-looped against nginx's :80/:443 for ~6 months (restart count 1489); when an ansible run bounced nginx (2026-06-11 09:34), caddy won the bind race, swallowed all tunnel traffic (external 308s/TLS errors, host.ycluster.net down ~15 min) until it was stopped (`docker update --restart=no caddy && docker stop caddy`) and nginx restarted. Remaining: (1) remove the stack entirely — operator confirmed 2026-06-11 it was an unused experiment (`docker compose down` in `/opt/src/searxng-docker`, then delete the dir; first check Open-WebUI has no web-search config pointing at it). Until then, only the stopped container's `restart=no` prevents a `compose up` from resurrecting caddy. (2) **Monitoring gap**: the failed-systemd-units alert only sees systemd units — a restart-looping docker container is invisible (1489 restarts, zero pages). Add container-state alerting (periodic `Restarting`/RestartCount check feeding the existing alert path, or cadvisor). (3) Minor certbot warts found in the same incident: `obtain` writes the new cert but doesn't reload nginx (manual reload needed on the obtain node), and `--test` deploys the *staging* cert to the live endpoint — fix or document both in `certbot_manager.py`.
- DONE: VM scheduling iteration 2: GPU release + admission control + commitments view (coupled — do not ship piecemeal; decided 2026-06-11, details in `docs/design/vm-usage-scheduling-accounts.md`). **Implemented + dev-tested 2026-06-12; rolled out to prod and live-verified on real passthrough hardware 2026-06-13** (reconciler logs show a scheduled stop releasing GPUs to the host pool and a later start re-attaching them by PCI address). (1) `release_gpus`: scheduled stops detach GPU devices to the pool, every start re-acquires the registered count via `free_gpus()` (raises rather than under-deliver); `ycluster vm release-gpus` for manual ops. (2) Save-time admission control in `/admin/vm-schedule/set` (409): commitments = registered GPUs always for unmanaged/on, never for off, windows for schedule; pool size from the sampler snapshot's new `gpu_pool` field (fails open until a host reports one); containers exempt. (3) Per-host commitments section on the schedule page. (4) Reconciler start/stop failures recorded to `/cluster/vm-issue/<name>` and shown on the VM's card (cleared on convergence/destroy/orphan sweep). Dev-verified: admission accept/reject incl. overlap + always-held baselines, fail-open, container exemption, owner-scoped views, issue record/clear lifecycle, scheduled stop+release path. Live-verified in prod 2026-06-13: real GPU detach on a scheduled stop and re-attach (by PCI address) on the next start, on a passthrough VM. Rolled out via install-ycluster-package.yml (all incus hosts) + setup-web-services.yml (storage).
- VM scheduling iteration 3: web-driven VM creation (design in `docs/design/vm-usage-scheduling-accounts.md` Phase 5; depends on iteration 2, which is done). Record the new VM in the existing `/cluster/vms/<name>` registry (no separate intent prefix) with a `state: requested` gate — the per-host vm-reconciler already iterates `vms_all()` filtered to its host, so it gains a branch: record present + no local incus instance + `state == requested` → create. Gate on `state` (requested → provisioning → ready), never on mere absence, or a hand-`incus delete`d VM with a stale record gets silently resurrected; a `ready`/running record whose instance vanished stays an anomaly (vm_start → "instance not found" → `/cluster/vm-issue/`), not a recreate. Key design decisions already made: (1) **provision GPU-less** — `vm_launch`'s slow tail (wait-agent, cloud-init, sshd, key inject, bastion sync) touches no GPU; the ~900 s wait is purely OVMF enumerating passed-through GPU BARs, so init without GPU devices boots in ~180 s, leaves the pool free during provisioning, and lands the VM **stopped with GPUs released** = the scheduler's steady state. GPUs attach only on the first real start via the existing `_ensure_gpus_attached`. (2) **detached provision task** — ~180 s still exceeds the 120 s tick / 300 s `TimeoutStartSec`, so the reconciler spins off a `vm-provision@<name>.service` (longer timeout) and returns; state field blocks re-entry; a crash leaves `provisioning` for an interrupted-recovery branch to clean up + relaunch. (3) Registry record gains `cpu`/`mem`/`image` (launch-args-only today). (4) Web picks `host` at create time using the iteration-2 pool sizes (`gpu_pool` in sampler snapshots) + `gpu_commitments`; same `gpu_conflict` admission check runs at create so over-capacity is rejected on the page. (5) **Admin-gated until quotas** (Phase 4) — no per-owner budget to admit self-serve creation against yet. Pre-flight: owner has SSH keys (vm_launch requires them), name free, capacity fits. Empirical check before relying on it: a guest provisioned with zero GPUs then booted with GPUs attached must bind its baked-in nvidia driver cleanly (should — same attach-then-boot ordering `_ensure_gpus_attached` + start already uses — but verify on a real passthrough VM).
- Drop the bcrypt hasher from authentik once imported hashes have converged. The 2026-06-11 Open-WebUI → authentik password migration (`ycluster user import-owui`) copied plain-bcrypt hashes, verified via the extra `BCryptPasswordHasher` in `config/ansible/app/files/authentik-user-settings.py`. Django re-hashes each user to PBKDF2 on their first successful authentik login, so the imported hashes disappear over time. Periodically check `select count(*) from authentik_core_user where password like 'bcrypt$%'` on the leader; when it reaches 0, remove the user_settings.py override (or trim it back to the stock list), the compose mounts, and the playbook task. Removing it earlier strands not-yet-logged-in users on an unverifiable hash (recoverable via reset link). Convergence here is also a good readiness signal for the OWUI `ENABLE_LOGIN_FORM=false` flip.
- Dev cluster: browsable external/TLS path (make reproducible). The forward-auth UX (redirect to authentik, login, bounce back) can't be exercised in a browser against dev — the public/app vhosts bind 127.0.0.2 only (the rathole endpoint, no analog on dev) and render HTTP-only (`web_services_force_local`), while authentik builds redirects as `https://<sub>.<domain>/...`. Headless curl coverage exists (cookie round-trip in the step-3 work); the app.<domain> work (2026-06-13) was validated end-to-end on dev only via **ad-hoc scaffolding** (a `.xc` override of the app provider's `external_host` + outpost `authentik_host`, plus a node-interface `app.xc` nginx vhost) — that scaffolding is NOT reproducible and `authentik-manager write-blueprints` resets the blueprint override, so this needs a proper fixture. Concrete design: (1) **drop the `.xc` override** — point the operator's `/etc/hosts` at the *real* dev subdomains (`app.<dev-domain>`/`auth.<dev-domain>`/`admin.<dev-domain>` → dev leader / storage VIP) so nginx server-names, the authentik providers, and the cert all already agree (no drift after `write-blueprints`); (2) **node-interface listen** — add a `listen <node>:443 ssl` to the public + app vhosts gated to the dev/`web_services_force_local` case (small dev-only template conditional) so the subdomains are reachable off 127.0.0.2; (3) **TLS via the existing pebble ACME** (`dev/cluster/acme-dev.yml` already stands up a local CA) so the dev names get a dev-trusted cert. Package as a `dev/cluster/` playbook + a doc line. NB the dev leader is s2 (not s1), and the storage VIP (10.0.0.100) follows it — `/etc/hosts` should target the VIP, not a fixed node.
- Make `ycluster https set-domain` print its blast-radius checklist. The external domain is start-time-dynamic, not watch-dynamic: authentik-manager renders the OWUI/admin blueprints and open-webui-manager its OIDC env from `/cluster/https/domain` at service start, and the cert SANs come from a certbot obtain. After a domain change the command should say: restart `authentik` and `open-webui` on the leader, run `ycluster certbot obtain` (on the rathole node), and re-run `certbot-manager update-nginx`.
- Make the storage-leader election's graceful teardown actually graceful (found 2026-06-12 via the dev cluster). `stop_all_services` in `config/ansible/storage/scripts/storage-leader-election` stops `ycluster-apps.target`/`docker-registry`/`rathole`/`misc-rbd`/`user-rbd` **in parallel** (5s-timeout background jobs) and never gracefully stops `postgresql@16-main`/`qdrant` at all — so the rbd units' plain `umount` always loses the race against the services still holding the mount (EBUSY, swallowed by `|| true`), the `pgrep postgres` check then always finds survivors, and EVERY failover takes the aggressive path: `pkill -9 postgres` + `xfs_io shutdown` + `umount -f -l` + force unmap. Postgres gets a crash-stop (WAL recovery on the next leader) on every routine drain. Fix shape: stop the mount *consumers* first and wait (postgresql@16-main, qdrant, ycluster-apps.target, docker-registry, rathole — bounded timeout), then stop `misc-rbd`/`user-rbd` so their umounts see a free mount; keep the aggressive path as the fallback it was meant to be. Evidence from dev: the parallel stop left a stale `/rbd/user` mount on the drained node (containers can't `umount -f -l`), which broke every `mountpoint -q` leader detection until swept — in prod the force-unmap hides the same race. The dev stand-in units work around it with an ExecStop umount retry loop (`dev/cluster/app-dev.yml`); fixing the election script's stop ordering would let prod failovers hand over a cleanly-shut-down postgres and make the dev workaround unnecessary. Test on the dev cluster (`system-test.sh` section 16 exercises exactly this path).
- vllm-mlx on m1 has a stack of upstream bugs and ad-hoc operational gaps (no `stop`, no guided decoding, broken special-token detokenization, no MiniMax tool-call parser, no pinned version, launch commands unscripted) — parked in `research/minimax-vllm-mlx-structured-output-2026-04-11.md` under "Upstream work queue" and "Operational gaps (m1)". Not actionable in the near term.
- Rebuild rathole with PROXY protocol support so the public admin-api (wg bootstrap + /status) sees real client IPs. Current rathole 0.5.0 is a plain L4 TCP tunnel — everything forwarded from the public host lands on 127.0.0.2 with source 127.0.0.1, so per-client rate limits and audit logs are impossible. nginx side would then use `listen 127.0.0.2:443 ssl proxy_protocol; set_real_ip_from 127.0.0.1; real_ip_header proxy_protocol;` once rathole emits the PP header. Upstream rathole doesn't support it — may need a fork/patch.
- Upstream the exo prompt-logging fix. exo leaks prompt content into its logs (and the on-disk log file) at INFO — `logger.info(prompt)` in `utils_mlx.py` plus `InputMessageContent.__repr__` showing the first 100 chars, which every task/command-object log dump recurses into. We carry a 2-hunk patch in `config/ansible/macos/files/exo/patches/0001-no-prompt-logging.patch`; submit it as a PR to exo-explore/exo so the carried patch can eventually be dropped.
- **s4 is intentionally drained — do not undrain until validated as a storage-leader.** s4 participates in Ceph/storage but has never been exercised as the storage leader (RBD mount of `/rbd/user`+`/rbd/misc`, postgres/qdrant/registry/rathole takeover, storage-VIP migration), so it's held out of leader election via `/cluster/nodes/s4/drain=true`. This is the correct steady state, not an anomaly — it was briefly undrained in error during the 2026-06-10 rolling reboot and re-drained. To make s4 leader-eligible: during a maintenance window, drain the current leader so leadership lands on s4, verify it mounts both RBD volumes cleanly, brings up all leader-only services, and that the storage VIP + external URL follow; only then leave it undrained. Overlaps the "core" consistency audit (is s4 core?) and the node-lifecycle runbook. Same s4 caveat as the gateway-VIP exclusion (now closed, see `CHANGELOG.md`) — s4 differs from s1-s3 in more than one subsystem.
- Make admin-api's cached etcd client fail over when its endpoint dies. `get_etcd_client()` (`config/ansible/admin/files/ycluster/ycluster/common/etcd_utils.py:86`) returns a process-wide `_CACHED_CLIENT`, and `connect_with_retry` pins it to the *first reachable* host in `ETCD_HOSTS` (`10.0.0.11,12,13` → normally s1). The python etcd3 client doesn't round-robin and the cache never reconnects, so draining whichever etcd node is first in the list blinds every long-running `_CACHED_CLIENT` consumer until restart. Observed 2026-06-17: draining s1 made the admin-api on the new storage leader (s2, on the storage VIP) throw `UNAVAILABLE 10.0.0.11:2381` on every read; the errors are swallowed into empty dicts, so `/api/cluster-status` returned `leadership:{}`, `hostHealth:{}`, VIP masters `null` with a *fresh* timestamp — making `ycluster health` report "no storage leader" / VIP "None" / "ceph unknown" even though storage had failed over to s2 cleanly. The takeover was fine; only the reporting was blind, which looks exactly like a stuck failover. Manual fix was `systemctl restart admin-api` on the leader (also self-heals once the dead node returns and its gRPC channel recovers). Fix shape: on `ConnectionFailedError`/`_InactiveRpcError`, drop and rebuild `_CACHED_CLIENT` against the remaining `ETCD_HOSTS` (or hand the whole list to a failover-capable client) so reads survive losing any single etcd node. Same root cause as the "global etcd client cached forever" note under Code review follow-ups → Minor.
- Consolidate redundant etcd reads in admin-api's health path (perf). On a core node, every `/metrics` scrape (frequent, per-node by Prometheus) makes ~6 separate etcd round-trips inside `get_comprehensive_health`: the `/test` probe, `is_storage_leader` (`/cluster/leader/app`), `is_dhcp_leader` (`/cluster/leader/dhcp`), `is_node_drained` (`/cluster/nodes/<h>/drain`), `check_certificate_expiry` (`/cluster/tls/cert`), and `get_core_nodes` → `get_all_hosts` (a full `by-hostname/` *prefix scan*). Fetch them in one batched/cached pass (e.g. a short TTL cache or a single multi-key read per scrape). Non-core nodes already make zero etcd calls here after the etcd-access-hardening Phase 1 gating; this is the core-node follow-up.
- Add periodic Docker image pruning on storage nodes (s1-s3). Dangling image layers from open-webui rebuilds accumulate in `/var/lib/docker/overlay2` and fill the root fs — on 2026-06-04 s2 hit `MON_DISK_LOW` (mon store lives on `/`, `/dev/mapper/vg0-root`) with ~50G of untagged `<none>` open-webui-with-plugins layers; `docker image prune -f` reclaimed it. Schedule a periodic `docker image prune -f` (dangling-only — never `-a`, which on a non-leader would delete the tagged-but-not-running open-webui `:latest` and force a ~4.4G re-pull on failover) via a systemd timer or Ansible-managed cron on each storage node. Note s2/s3 root disks (197G) are smaller than s1 (295G) so they hit the 30% `mon_data_avail_warn` threshold sooner. Registry blobs are unaffected by pruning — they live on RBD at `/rbd/misc/docker-registry/data`, outside Docker's image store.
- Per-node uplink failover watchdog. A core node whose direct uplink's WAN is dead but whose next-hop is still ARP-reachable keeps its metric-100 DHCP default route and blackholes egress instead of falling back to the gateway VIP (`.254`, metric 500) — the kernel only drops a route on carrier/next-hop loss, not on dead-WAN-behind-a-live-router. Caused an `admin.host` outage after the 2026-06-17 cold start: s2/s3's `192.168.0.1` uplink didn't recover, the storage leader (s2) had no internet, and its rathole client couldn't resolve `host.ycluster.net` to establish the tunnel (fixed by hand with `ip route del default via 192.168.0.1`). Fix: a small per-node systemd-timer watchdog that probes the internet *forced through the own uplink* (reuse `gateway_health.py`'s `SO_BINDTODEVICE` probe so it still detects recovery after failover) and, with hysteresis, bumps the own-uplink default route's metric above the `.254` fallback when dead / restores metric 100 when healthy. Composes with the gateway VIP, which already lands on a healthy-uplink node (so the `.254` a demoted node leans on is guaranteed to NAT out a working uplink); the VIP holder's uplink is healthy by definition, so the same one rule needs no special-casing. **The bad route is DHCP-owned by systemd-networkd** (`proto dhcp`, metric 100, rendered from `netplan-cluster.yaml.j2`), and networkd re-asserts it on every lease renew (T1 ≈ 54min on the observed 2h lease), on any reconfigure, and on reboot — so a manual `ip route del`/metric change loses to networkd (confirmed 2026-06-17: a hand `del` held ~20min until the T1 renew re-added it). The watchdog must therefore NOT fight networkd's main-table route; use one of: (a) a watchdog-OWNED lower-metric default via `.254` toggled on health (e.g. `metric 50` when the uplink is dead so it beats the DHCP `metric 100`, withdrawn when healthy) — the same shape as the 2026-06-17 stopgap `ip route add default via 10.0.0.254 dev enp2s0f0np0 metric 50` (survives renews but not reboot); or (b) policy routing (`ip rule` selecting an uplink table vs a gateway table), independent of the networkd-managed route. Add a note to the cold-start runbook (`docs/operations/disaster-recovery.md`).
- WG inbound ingress needs a stable externally-routable endpoint that follows the active leader. Currently the DNAT on the upstream router points at a specific core node's uplink IP (e.g. `192.168.0.104` = s3), so wg breaks on gateway-VIP failover to s1/s2. Options to investigate: (1) uplink-side VIP via keepalived on the 192.168.0.x segment — clean but only s2/s3 are on that subnet (s1 is on 192.168.9.x), so s1 can't participate and gateway-VIP leadership would need to be constrained to s2/s3; (2) static route `10.0.0.0/24 via <core node>` on the router so DNAT can target `10.0.0.254` directly — simplest but single-next-hop fragility; (3) a tiny UDP forwarder on each core node that knows which peer currently holds the gateway VIP and proxies wg packets there. Option 1 is probably the right call once s1's uplink is reconciled.
- `ycluster wg delete <hostname>` cascades into the node allocation, with no peer-only variant. `delete_peer` (`config/ansible/admin/files/ycluster/ycluster/utils/wg_config.py:183`) removes the wg peer *and* `/cluster/nodes/by-hostname/<h>` + `by-mac/<mac>` (symmetric with wg register, which created both), so once a node is re-bootstrapped onto the LAN, `wg delete` silently drops its live allocation — gone from the status page, loses its DHCP reservation. Bit us 2026-06-17 cleaning up nv1's stale peer; recovered by re-running the nvidia bootstrap. Root cause underneath: a wg peer's AllowedIPs follows the node's allocation IP, so after a wg→LAN migration the peer carries a `10.0.0.x/32` that the gateway-VIP holder (the only node running wg0) installs as `10.0.0.x dev wg0`, black-holing the node's return traffic into the dead tunnel — which is what made nv1 unreachable from whichever node held the VIP. Two fixes: (1) give the wg layer a peer-only removal (or have it refuse/auto-clean peers whose AllowedIPs fall in `10.0.0.0/24`); (2) make `dhcp delete` cascade to the wg peer so one command cleans a migrated node. Operational workaround is documented in `docs/operations/wireguard.md` (`wg revoke`, which keeps the allocation, + a manual `ip route del <ip> dev wg0` since `syncconf` leaves the wg-quick route behind).

## Production readiness gaps (2026-05-22 audit)

### Disaster recovery & backups
- Write disaster-recovery runbooks for the cases the existing docs punt on: etcd quorum loss (minority survivors), Ceph quorum loss / corruption, full cluster cold-start, VIP split-brain recovery. `docs/operations/etcd.md` and `ceph.md` explicitly reference DR procedures that don't exist.
- Add a `restore` subcommand to `config/ansible/storage/scripts/backup-databases` (or sibling script) covering pg_dumpall, Qdrant snapshot, and `etcdctl snapshot restore`. Document the procedure end-to-end including age decryption.
- Periodic test-restore job: decrypt the latest backup to a scratch location and verify pg/qdrant/etcd load cleanly. Alert on failure or stale (>N day) backups. Two-layer approach to avoid widening attack surface:
  - **Nightly automated, on the storage leader.** Keep an age privkey on the storage leader (root-only, same FS as live pg/etcd data — putting it there doesn't expand blast radius since the host already holds plaintext) and restore the latest backup into a scratch pg cluster on a non-default port + a temp qdrant + an etcd snapshot dir. Smoke-query, then wipe. Caveat: enforce strict mode/ownership on the privkey file in the playbook (root:root 0400, on the same encrypted FS as the DB) — its "no extra surface" property depends on that.
  - **Quarterly attended drill with a hardware key.** Use `age-plugin-yubikey` (or TPM-backed) so the privkey never lives on disk anywhere. Operator plugs the token into a clean drill host, decrypts the latest offsite backup, restores end-to-end, runs smoke checks. This is what actually proves we can recover from compromise/loss of the storage leader. Token lives in a safe between drills; document key custody and a reissuance procedure for when an operator leaves.
- Ceph off-cluster backup: RBD (`rbd/user`/`rbd/misc`) has no off-cluster copy. The DB dumps cover the stateful services; the remaining at-risk content is the docker registry blobs (`/rbd/misc/docker-registry`) and any hand-placed files. Implement `rbd export-diff` → age-encrypt → existing offsite destinations, scheduled with `backup-databases` (design + scope in `docs/operations/disaster-recovery.md#ceph-off-cluster-backup-story`).
- Document age private-key custody (where the recipient privkeys live off-cluster, who can decrypt, rotation procedure).

### Node lifecycle
- End-to-end "replace a dead node" runbook: PXE → autoinstall → etcd rejoin → Ceph rejoin → service handoff, with explicit checkpoints.
- Graceful decommission runbook (drain Ceph OSDs, leave etcd, retire from inventory).
- `ycluster node {add,remove,drain}` CLI wrappers around the above so it's not a manual ssh+sudo dance during an outage.
- `ycluster failover` to manually move storage-leader / gateway VIP for maintenance.

### Monitoring gaps
- GPU health: temperature, ECC errors, memory, FLR events. The 2026-05-21 nv3 cascade (NOTES.md) wouldn't have been caught.
- Incus / VM guest disk-full alerts (root cause of the same cascade).
- Postgres and Qdrant liveness alerts (currently only etcd, Ceph, DHCP, inference are watched).
- Backup freshness / rsync-success metric + alert.
- Clock-skew alert rule (README claims it; not present in `ycluster-alerts.yml.j2`).
- Ansible-run-success metric so "did the last apply succeed on every host" isn't a manual SSH check.
- The failed-systemd-units alert (`SystemdUnitFailed`, done 2026-06-10 — see `CHANGELOG.md`) partly subsumes the per-service liveness and ansible-run-success items above. Follow-on: the nv1 autossh-tunnel tunnel it caught was fixed in place but isn't repo-managed — consider ansible-izing it.

### CI / testing
- ansible-lint in a GitHub Action gating `main`.
- `ansible-playbook --check` smoke run against a representative inventory subset.
- Pre-commit hook for yaml/jinja lint.

### Incus storage migration (urgent, from NOTES.md)
- Migrate Incus storage pools from `dir` to `zfs` with per-VM `refreservation` so a runaway guest can't fill the host root fs and trigger the vm1/GPU-FLR cascade seen on nv3 2026-05-21. Write the migration procedure (it's destructive — VMs get recreated).

### Known issues in NOTES.md to convert into tickets or runbook entries
- open-webui stops immediately after leader election (2025-10-09).
- meshcommander KVM/SOL broken; Ubuntu pass-change broken.
- Missing `docker.gpg.asc` on some hosts.
- Squid slow-DNS regression re-applied by Ansible after manual fix.
- ollama memory plugin failure with no graceful degradation.
- "redo s3" — clarify scope (Ceph RGW?) and either fix or file.

### Scaling / capacity docs
- Add-storage-node and add-compute-node procedures (Ceph rebalance expectations, OSD onboarding, network/VLAN, GPU setup where relevant).
- Capacity-planning notes: Ceph write-perf cliff at ~85% (empirical, in NOTES), recommended free-space margins, rebalance time estimates.

## Code review follow-ups (2026-06-09)

From `docs/reviews/2026-06-09-codebase-review.md` (IDs reference that doc; line numbers are vs commit f1e3305). Items already tracked elsewhere in this file are not duplicated here: CA-off-RBD (Security), rathole PROXY protocol, gateway health check, etcd read consolidation.

Verified bugs B1–B8 and the Admin API hardening items (S1/S2/S3) are done — see `CHANGELOG.md`. Follow-on idea kept from S2: have allocation issue each node a client cert from the cluster CA, giving provisioned nodes an identity for any future authenticated calls.

### Architecture
- A1 — bootstrap is trust-on-first-use keyed to MAC addresses (rogue LAN device can PXE-join as any node type; `/bootstrap/*` served unsigned to `sudo bash`). Document the LAN/TOFU trust model in ARCHITECTURE.md; cheap hardening: MAC-OUI validation + checksum-verified bootstrap scripts.
- A2 — API keys stored plaintext (etcd master key; Open-WebUI `api_key` rows). Threat-model item; hashing the OWUI keys needs upstream changes.
- A3 — setup-time RBD maps lack `--exclusive` (`storage/setup-user-rbd.yml:68`, `storage/setup-misc-rbd.yml:29`) though runtime mounts have it; add for consistency.
- A4 — Go proxy etcd-watch isn't restarted on break (`source.go:134`) so model config silently stops hot-reloading; disabled-backends set goes stale on etcd outage (`disabled.go:32`). Add watch-error → re-list-and-rewatch.

### Ansible hygiene
- H1 — `groups['storage'][0]` delegation (~20× in `storage/setup-user-rbd.yml`) breaks under `--limit`; use the mountpoint-based leader-detection convention.
- H2 — `ignore_errors: yes` on stop/teardown paths (`wipe-etcd.yml`, `storage/stop-storage-leader-election.yml`) hides hung services before destructive steps; use `failed_when:` with explicit checks.
- H3 — systemd units for the DHCP server and admin-api have no hardening directives even where root is required for raw sockets.

### Minor
- `install-ycluster-package.yml` restarts admin-api when the package updates but not dhcp-server, so the DHCP leader keeps running old package code until something else restarts it (had to restart it by hand after the 2026-06-10 B3 rollout). Add dhcp-server (when active) to the package-update restart, like the etcd-endpoint-change handler already does.
- `/api/allocate` hardcodes `"existing": true` in its response, so callers can't tell a lookup from a fresh allocation — on 2026-06-10 this disguised an accidental allocation as a read during canary testing and fired a node-down alert. Return the real created/existing state (get_or_create_allocation knows it) and consider a `dry_run`/lookup-only query param for diagnostics.
- Go: health `Probe()` goroutines use bare `context.Background()` with no timeout (`health.go`); 4xx bodies only partially drained on retry (`handler.go:329`); ACL is allow-by-default for unlisted models (consider a deny-unknown mode).
- Python: global etcd client cached forever (`etcd_utils.py:86`), stale after member changes; mixed `print(file=sys.stderr)` vs logging in app.py; MAC normalization duplicated across ~5 files; nginx `-t` stderr discarded in `certbot_manager.py:215`.
- `/api/allocations` and `/api/health` expose full topology/health detail unauthenticated on the cluster network (recon value; acceptable under the LAN trust model once A1 is stated).

### Security
- Clean up stale/vendor-created user accounts on managed nodes. In particular `vendoracct` on nv2 (Vendor factory-preinstall account, audited 2026-06-09): full sudo (`(ALL : ALL) ALL`), in the `lxd` group (root-equivalent), and a lingering login session (two bash shells + pipewire user services running since ~2026-05-22). Interim mitigation applied 2026-06-09: password locked (`passwd -l`) and `authorized_keys` renamed to `.disabled` (it held the cluster `ansible@pxe-server` key); SSH login verified refused. Note this was done ad-hoc on nv2 — a fresh vendor image would come up unmitigated. It also squats UID 1000, which is why `dev` is UID 1002 on nv2 instead of the cluster-standard 1000. Cleanup: verify nothing references `vendoracct` (cron, services, file ownership under /home/vendoracct), kill its sessions, `deluser --remove-home`; decide whether to renumber `dev` to UID 1000 (chown sweep) or leave it. Then sweep the other non-PXE node types (nvidia, nas, macos) for similar vendor/installer accounts and fold the check into their bootstrap playbooks.
- SSH-key rotation procedure (Ansible key at `/data/ansible_ssh_key` and any node-to-node keys).
- Secrets-rotation flow for things stored in etcd (`/cluster/config/inference/master-key` and similar) and in vault.
- Audit-log story: journald aggregation, etcd/Ceph audit logs, retention.
- Reconsider how `become` is used across the playbooks, prompted by two macOS findings (2026-06-13, provisioning the first wg-joined Mac, m3). (1) **`raw` + `become` cannot take a multiline command.** Confirmed by isolation on m3: trivial+become works, `if/fi` on one physical line + become works, but a real-newline (YAML `|` block) command fails with `/bin/sh: -c: line 1: syntax error: unexpected end of file`. The `-i` login-shell flag in `group_vars/macos.yml` (`ansible_become_flags: "-H -i"`) is *not* the cause. So any future `raw` task under become must be a single physical line (folded `>-` scalar, `||`-grouped, no embedded single quotes). (2) **The macOS play's become model is muddled.** It runs `become: yes` (admin→root) at play level but flips to `become_user: dev` for Homebrew tasks, and assumes Homebrew lives at `/opt/homebrew` owned by `dev` — true only when the *playbook* installed brew (clone + chown-to-dev). On a wg-joined Mac the human pre-installs brew (the wg bootstrap's prerequisite) under their own admin account, so `homebrew_check.stat.exists` is true, the clone+chown tasks skip, and `brew install` as `dev` fails ("directories are not writable by your user: /opt/homebrew ..."). Decide the intended ownership/become model: either normalize brew ownership to `dev` regardless of who installed it (a chown task that runs even when brew pre-exists), or run brew as whatever account owns the prefix, or standardize brew installation so LAN and wg paths converge. Audit the `become`/`become_user`/`-i` usage globally while here.
